pipeline:
  chunking: semantic
  embeddings: huggingface
  vector_store: faiss
  retrieval: semantic
  llm: gemini
params:
  chunk_size: 1000
  chunk_overlap: 100
  top_k: 5
  use_reranker: true
  use_agentic: true
